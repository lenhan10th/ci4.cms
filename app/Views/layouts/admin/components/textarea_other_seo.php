<div class="form-group">
    <label class="control-label">Other (Chỉ dùng cho code CSS & JS)</label>
    <textarea id="other_seo" name="other_seo" placeholder="Chỉnh code css, js (nếu có)..."><?php echo isset($row['other_seo']) ? html_escape($row['other_seo']) : ''; ?></textarea>
</div>